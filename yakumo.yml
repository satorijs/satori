- name: yakumo
  config:
    pipeline:
      build:
        - tsc
        - esbuild
      clean:
        - tsc --clean
    alias:
      server: /external/server/packages/core
      satori: /packages/core
      ss: /packages/server
      sa: /adapters/satori
      lark: /adapters/lark
- name: yakumo-esbuild
- name: yakumo-tsc
